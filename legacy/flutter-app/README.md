# Legacy Flutter app (archived)

The original Kariera app — **Flutter + Firebase** — kept here for reference only.

- **Not built or maintained.** New tooling (lint/test/build, Docker) targets
  `backend/` and `apps/` only.
- Superseded by: `mobile/` (React Native + Expo) and `backend/` (Django + DRF).
- See [`../../docs/00-current-audit.md`](../../docs/00-current-audit.md) for the
  audit, what was reused/rebuilt, and the migration plan.

Moved here via `git mv` (history preserved). Safe to delete once the rebuild is
confirmed and any needed assets/data have been migrated.
