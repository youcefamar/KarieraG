# admin-web (placeholder)

Future custom admin/analytics dashboard for Kariera.

**Status:** placeholder only. For MVP 1–2 the admin experience is **Django Admin**
(`/admin/`). This app will be built starting MVP 3 when analytics and institute
management need a richer UI than Django Admin provides.

Likely stack (to be decided): Next.js + TypeScript, sharing the same
`/api/v1/` backend and JWT auth as the mobile app.

Do not add code here yet.
